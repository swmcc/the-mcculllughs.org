<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "the-mcculloughs.org | 1933 – #{Date.current.year}" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-neutral-50 min-h-screen text-neutral-900 antialiased flex flex-col">
    <% if user_signed_in? %>
      <nav class="bg-black/80 backdrop-blur-md border-b border-white/10 fixed top-0 w-full z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-14">
            <div class="flex items-center">
              <%= link_to root_path, class: "flex items-center group" do %>
                <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-purple-600 rounded-lg flex items-center justify-center hover:scale-105 transition-transform">
                  <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                  </svg>
                </div>
              <% end %>
            </div>

            <!-- Search -->
            <div class="flex-1 max-w-lg mx-6">
              <%= form_with url: search_path, method: :get, local: true, class: "relative group" do %>
                <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                  <span class="text-lg">🔍</span>
                </div>
                <input type="text"
                       name="q"
                       value="<%= params[:q] %>"
                       placeholder="Search photos, galleries, dates..."
                       class="w-full bg-gradient-to-r from-white/10 to-white/5 border border-white/20 rounded-xl pl-12 pr-4 py-2.5 text-sm text-white placeholder-white/50 shadow-lg focus:outline-none focus:from-white/15 focus:to-white/10 focus:border-white/30 focus:shadow-xl transition-all">
              <% end %>
            </div>

            <div class="flex items-center space-x-3">
              <div class="relative" data-controller="dropdown">
                <button data-action="click->dropdown#toggle" class="flex items-center space-x-2 text-sm text-white/70 hover:text-white transition-colors p-1 rounded-lg hover:bg-white/10">
                  <div class="w-7 h-7 bg-gradient-to-br from-purple-500 to-pink-500 rounded-full flex items-center justify-center">
                    <span class="text-xs font-medium"><%= current_user.name.first %></span>
                  </div>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>
                <div data-dropdown-target="menu" class="hidden absolute right-0 mt-2 w-48 bg-neutral-800 rounded-lg shadow-xl border border-white/10 py-1">
                  <%= link_to edit_user_registration_path, class: "block px-4 py-2 text-sm text-white/70 hover:text-white hover:bg-white/5 transition-colors" do %>
                    Settings
                  <% end %>
                  <div class="border-t border-white/10 my-1"></div>
                  <%= button_to destroy_user_session_path, method: :delete, class: "w-full text-left px-4 py-2 text-sm text-white/70 hover:text-white hover:bg-white/5 transition-colors" do %>
                    Sign Out
                  <% end %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <% if notice.present? || alert.present? %>
      <div class="<%= user_signed_in? ? 'mt-14' : '' %> max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4" data-controller="notification">
        <% if notice %>
          <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg mb-4">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg mb-4">
            <%= alert %>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="<%= user_signed_in? ? 'pt-14' : '' %> flex-1">
      <%= yield %>
    </main>

    <% if user_signed_in? %>
      <%= render "shared/footer" %>
    <% end %>
  </body>
</html>
